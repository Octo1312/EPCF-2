{% extends 'base.html.twig' %}

{% block title %} MyCustomCard - Galerie {% endblock %}

{% block body %}
<section id="museum">
    <div class="container">
        <div class="cards container">
        {% for card in cards %}
            <div class="card">
            <div class="card-frame">
                <img src="{{ asset('asset/img/' ~ card.color.frame) }}" alt="Frame card">
            </div>
            <div class="card-stats">
                <img src="{{ asset('asset/img/' ~ card.color.stats) }}" alt="stats card">
            </div>
            <div class="card-img">
                <img src="{{ asset('asset/uploads/images/' ~ card.imageName) }}" alt="Img card">
            </div>
            <div class="card-title">        
                <h2>{{ card.name }}</h2>
            </div>
            <div class="card-effect">
                <p>{{ card.effect }}</p>
            </div>
            <div class="card-power">
                <p>{{ card.strength }}/ </p>
                <p>{{ card.toughness }}</p>
            </div>
            </div>
        {% endfor %}    
        </div>
    </div>
</section>
{% endblock %}
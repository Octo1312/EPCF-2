{% extends 'base.html.twig' %}

{% block title %}Créer une carte !{% endblock %}

{% block main %}
    <section id="card">
        <div class="container">
                <div class="container-form">
                    <h1> Créer une carte </h1>

                    {{form_start(addcard)}}
                        <div class="form">
                            <button> Ajouter </button>
                        </div>
                    {{form_end(addcard)}}
                  </div>
            </div>
    </section>

{% endblock %}

{% extends 'base.html.twig' %}

{% block title %} Assistant de création de cartes - Étape 2 {% endblock %}

{% block body %}
<section id="type">
    <div class="form container">
{{ form_start(steptwo) }}
    {{form_row(steptwo.type, {
        'label': 'Type de votre carte'
    }) }}
    <div class="formbtn">
        <button class="btn" type="submit">Étape suivante</button>
    </div>
{{ form_end(steptwo) }}
</section>
{% endblock %}
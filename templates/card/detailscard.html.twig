{% extends 'base.html.twig' %}

{% block title %} MyCustomCard - Détails de la carte {% endblock %}

{% block body %}
<div class="card-title">        
    <h2>{{ card.name }}</h2>
</div>
<div class="card-effect">
    <p>{{ card.effect }}</p>
</div>
<div class="card-power">
    <p>{{ card.strength }}</p>
    <p>{{ card.toughness }}</p>
</div>
{% endblock %}